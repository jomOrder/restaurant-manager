version: '3'

services:
  bayzlink:
    build: .
    container_name: veggible
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    ports:
      - '8080:8080'
    networks:
      - app-network
    command: npm run start:local


#Docker Networks
networks:
  app-network:
    driver: bridge
